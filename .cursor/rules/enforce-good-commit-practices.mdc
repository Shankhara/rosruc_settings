---
description: Aide-moi à faire de bons commits : un seul changement par commit, en anglais, clair et court.
globs: **/*
alwaysApply: true
---
Description : Aide-moi à faire de bons commits : un seul changement par commit, en anglais, clair et court.  

Détails : Propose un découpage (ex. UI vs logique), préviens quand committer, et donne des messages comme "Add EventItem component".  

Exemple :
Changements :
```tsx
const EventItem = ({ event }) => <Text>{event.title}</Text>;
const { data } = useQuery({ queryKey: ['events'], queryFn: fetchEvents });
```

Suggestion :
Commit 1 : "Add EventItem component for event display"
Commit 2 : "Update fetchEvents logic with React Query"
Avertissement : "Sépare UI et logique en deux commits." 